# Warm editorial theme. Cohesive look regardless of the viewer's light/dark
# setting. (secrets.toml lives next to this and is gitignored.)
[theme]
base = "light"
primaryColor = "#c0573c"
backgroundColor = "#f7f3ec"
secondaryBackgroundColor = "#efe7da"
textColor = "#23201b"

[server]
maxUploadSize = 200
